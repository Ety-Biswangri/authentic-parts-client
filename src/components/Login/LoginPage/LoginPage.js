import React from 'react';
import { useForm } from "react-hook-form";

const LoginPage = () => {
    return (
        <div>
            <h2>Please Login</h2>
        </div>
    );
};

export default LoginPage;